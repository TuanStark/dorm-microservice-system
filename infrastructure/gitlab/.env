# GitLab Environment Configuration

# GitLab Configuration
GITLAB_HOSTNAME=localhost
GITLAB_HTTP_PORT=8082
GITLAB_SSH_PORT=2224
GITLAB_IP=10.0.0.18

# Database Configuration (optional - sử dụng PostgreSQL built-in)
# POSTGRES_DB=gitlab
# POSTGRES_USER=gitlab
# POSTGRES_PASSWORD=your_password

# Redis Configuration (optional - sử dụng Redis built-in)
# REDIS_PASSWORD=your_redis_password

# SMTP Configuration (optional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
# SMTP_DOMAIN=gmail.com

# Backup Configuration
BACKUP_KEEP_TIME=604800
BACKUP_PATH=/var/opt/gitlab/backups

# Performance Configuration
PUMA_WORKER_PROCESSES=2
SIDEKIQ_MAX_CONCURRENCY=20
POSTGRESQL_SHARED_BUFFERS=256MB
REDIS_MAXMEMORY=256m
